// write a programe to check if both variable are same or not if same then print raju is young. if not raju is old
#include<stdio.h>
void main()
{
    int a,b;
    printf("value of a");
    scanf("%d",&a);
    printf("value of b");
    scanf("%d",&b);
    if(a==b)
    {
        printf("raju is young");
    }
    else
    {
        printf("raju is old");
    }
    printf("\ngoodbye");
}